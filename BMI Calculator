function bmiCalculator(weight,height){
    var bmi = weight/(height**2);
    return bmi;
    //console.log("The BMI is supposed to be, "+ bmi);
}
//bmiCalculator(65,1.8);
var bmi=bmiCalculator(65,1.8);
console.log(Math.round(bmi));
